name: 📚 Docs

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev
      - docs

jobs:
  deploy:
    name: 🖋 Build and deploy docs
    uses: quaternionmedia/.github/.github/workflows/docs.yml@pdm
    with:
      pdm_args: -G docs --no-default
