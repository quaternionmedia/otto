name: 📦 Release

on:
  milestone:
    types: [closed]

jobs:
  release:
    name: 📦 Create draft release from milestone
    uses: quaternionmedia/.github/.github/workflows/draft_release.yml@main
